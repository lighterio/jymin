var focusedElement;on('', 'focus', function (element) {  var focusMethod = element.focus;  if (focusMethod) {    focusedElement = element;    removeTimeout(focusMethod);  }});on('', 'blur', function (element) {  var focusMethod = element.focus;  if (focusMethod) {    addTimeout(focusMethod, function () {      if (focusedElement == element) {        focusedElement = null;      }    });  }});